export interface Protocol {
  id: string;
  name: string;
  stateId: string;
  apy?: string;
  displayName: string;
  description?: string;
  iconUrl?: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}
